{
  "name": "tailwindcss-glow-example",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "tailwindcss -i input.css -o out.css --watch",
    "build": "tailwindcss -i input.css -o out.css --minify"
  },
  "dependencies": {
    "tailwindcss": "^4.3.3",
    "tailwindcss-glow": "link:.."
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.3"
  }
}
